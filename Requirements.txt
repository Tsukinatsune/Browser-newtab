# requirements.txt — tripo3d_to_imgbb.py
# Install with:  pip install -r requirements.txt
#
# Python 3.9+ required (uses str | None union syntax)

# Desktop webview window (wraps OS native browser: Edge on Windows, WebKit on macOS/Linux)
pywebview>=5.0,<7.0

# Image creation & pixel encoding
Pillow>=10.0,<13.0

# HTTP upload to imgbb
requests>=2.28,<3.0
